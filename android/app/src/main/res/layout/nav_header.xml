<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="bottom"
    android:padding="16dp"
    android:background="@color/brand_600">

    <TextView
        android:id="@+id/tvUserName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium"
        android:text="Dr. Dupont" />

    <TextView
        android:id="@+id/tvUserRole"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/brand_200"
        android:textSize="13sp"
        android:layout_marginTop="2dp"
        android:text="Medecin" />

    <TextView
        android:id="@+id/tvUserService"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/brand_300"
        android:textSize="12sp"
        android:layout_marginTop="2dp"
        android:text="Pharmacie" />

    <Spinner
        android:id="@+id/spEntity"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/spinner_entity_bg"
        android:popupBackground="@color/brand_700"
        android:dropDownWidth="match_parent" />

</LinearLayout>